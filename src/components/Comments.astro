<section class="comments" aria-label="댓글">
  <h2>정정 · 제보</h2>
  <p class="comments__hint">정정·제보는 댓글로 남겨 주세요.</p>
  <div class="comments__widget">
    <button type="button" class="comments__load" data-load-comments>
      댓글 불러오기
      <span class="comments__load-note">(외부 서비스 utterances를 불러옵니다)</span>
    </button>
  </div>
</section>

<style>
  .comments { margin-top: var(--space-5); padding-top: var(--space-4); border-top: 1px solid var(--border); }
  .comments h2 { font-size: 1rem; }
  .comments__hint { color: var(--fg-muted); font-size: 0.9rem; }
  .comments__widget {
    min-height: 260px;
    display: flex;
    align-items: flex-start;
    justify-content: center;
    padding-top: var(--space-4);
  }
  .comments__load {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 10px 20px;
    background: var(--surface);
    color: var(--fg);
    font: inherit;
    font-size: 0.95rem;
    cursor: pointer;
  }
  .comments__load:hover { border-color: var(--fg-muted); }
  .comments__load:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }
  .comments__load-note { color: var(--fg-muted); font-size: 0.78rem; }
</style>
