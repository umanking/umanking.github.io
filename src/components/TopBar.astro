---
---

<header class="topbar">
  <div class="topbar__inner">
    <a class="topbar__brand" href="/">CodeNexus</a>

    <div class="topbar__search" title="검색은 준비 중입니다">
      <label class="sr-only" for="topbar-search">검색</label>
      <input id="topbar-search" type="search" placeholder="검색 (준비 중)" disabled />
    </div>

    <button
      id="theme-toggle"
      class="topbar__theme"
      type="button"
      aria-label="라이트/다크 테마 전환"
      aria-pressed="false"
    >
      <span class="icon-sun" aria-hidden="true">☀</span>
      <span class="icon-moon" aria-hidden="true">☾</span>
    </button>
  </div>
</header>

<script is:inline>
  (function () {
    var btn = document.getElementById("theme-toggle");
    if (!btn) return;
    var root = document.documentElement;
    function isDark() {
      var current = root.getAttribute("data-theme");
      return current
        ? current === "dark"
        : window.matchMedia("(prefers-color-scheme: dark)").matches;
    }
    // aria-pressed는 "현재 다크 모드인가"를 반영한다 — 클릭 후에도
    // 스크린리더가 버튼 자체에서 현재 테마 상태를 알 수 있게 한다.
    btn.setAttribute("aria-pressed", String(isDark()));
    btn.addEventListener("click", function () {
      var next = isDark() ? "light" : "dark";
      root.setAttribute("data-theme", next);
      try {
        localStorage.setItem("theme", next);
      } catch (e) {
        /* 스토리지 접근 불가 환경(사생활 보호 모드 등)은 조용히 무시한다 */
      }
      btn.setAttribute("aria-pressed", String(isDark()));
    });
  })();
</script>

<style>
  .topbar {
    position: sticky;
    top: 0;
    z-index: 60;
    background: var(--bg);
    border-bottom: 1px solid var(--border);
  }
  .topbar__inner {
    max-width: 1360px;
    margin: 0 auto;
    padding: 0 var(--space-3);
    min-height: 56px;
    display: flex;
    align-items: center;
    gap: var(--space-4);
  }
  .topbar__brand {
    font-weight: 700;
    font-size: 1.1rem;
    letter-spacing: -0.01em;
    color: var(--fg);
    text-decoration: none;
  }
  .topbar__search {
    flex: 1;
    max-width: 420px;
  }
  .topbar__search input {
    width: 100%;
    padding: 8px 12px;
    border-radius: 8px;
    border: 1px solid var(--border);
    background: var(--bg-subtle);
    color: var(--fg-muted);
    font-size: 0.9rem;
  }
  .topbar__search input:disabled {
    cursor: not-allowed;
  }
  .topbar__theme {
    margin-left: auto;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--surface);
    color: var(--fg);
    font-size: 1rem;
    cursor: pointer;
  }
  .topbar__theme:hover {
    border-color: var(--accent);
    color: var(--accent);
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0 0 0 0);
  }
  @media (max-width: 640px) {
    .topbar__search {
      display: none;
    }
  }
</style>
